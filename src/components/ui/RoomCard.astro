---
import type { RoomPageContent } from "../../content/types";
import NationalTravelSubsidyTag from "../NationalTravelSubsidyTag.astro";
import CtaLink from "./CtaLink.astro";

interface Props {
  room: RoomPageContent;
}

const { room } = Astro.props;
const image = room.photos[0];
const roomHref = `/rooms/${room.slug}`;
---

<article class="room-card">
  {
    image && (
      <a class="room-card-image-link" href={roomHref} aria-label={`查看${room.name}房型`}>
        <img src={image.src} alt={image.alt} />
      </a>
    )
  }
  <div class="room-card-body">
    <div class="room-card-heading-row">
      <p class="room-meta">建議 {room.capacity.standard} 人｜{room.bedSetup}</p>
      <NationalTravelSubsidyTag variant="lovetour" />
    </div>
    <h2>{room.name}</h2>
    <p>{room.summary}</p>
    <div class="chips">
      {room.featureHighlights.slice(0, 3).map((feature) => <span>{feature}</span>)}
    </div>
    <div class="actions">
      <CtaLink label="查看房型" href={roomHref} variant="outline" tone="primary" size="sm" />
    </div>
  </div>
</article>

<style>
  .room-card {
    overflow: hidden;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    background: var(--color-surface);
    box-shadow: var(--shadow-sm);
  }

  .room-card-image-link {
    display: block;
    color: inherit;
    text-decoration: none;
  }

  img {
    display: block;
    width: 100%;
    aspect-ratio: 4 / 3;
    object-fit: cover;
  }

  .room-card-body {
    display: grid;
    gap: var(--space-4);
    padding: var(--space-6);
  }

  .room-meta {
    margin: 0;
    color: var(--color-primary);
    font-size: var(--font-size-sm);
    font-weight: var(--font-weight-black);
  }

  .room-card-heading-row {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-2);
  }

  h2 {
    margin: 0;
    font-size: var(--font-size-xl);
    line-height: var(--line-height-heading);
  }

  p {
    margin: 0;
    color: var(--color-text-secondary);
  }

  .chips,
  .actions {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2);
  }

  .chips span {
    padding: var(--space-2) var(--space-3);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-pill);
    color: var(--color-text-secondary);
    font-size: var(--font-size-sm);
    background: var(--color-background);
  }
</style>
